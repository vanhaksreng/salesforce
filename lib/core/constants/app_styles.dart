import 'package:flutter/material.dart';
import 'package:salesforce/theme/app_colors.dart';

const appBarIconSize = 24.0;
const appSpace = 15.0;
const appSpace8 = 8.0;
const appRounding = Radius.circular(8);
const appElevation = 4.0;
const tableHeaderHieght = 40;
const tableRowHieght = 70;
const double heightBottomSearch = 60;
const searchBgColor = Color.fromRGBO(255, 255, 255, 0.15);
const borderClr = Color.fromRGBO(255, 255, 255, 0.5);

const Gradient linearGradient = LinearGradient(colors: [mainColor, mainColor50]);

const Gradient linearGradient50 = LinearGradient(colors: [Color(0xFF667EEA), Color(0xFF764BA2)]);

const double appBtnRound = 10.0;
