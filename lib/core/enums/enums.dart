enum MessageStatus { success, warning, errors }

enum ServerType { shared, dedicated }

enum TableType { master, transaction }

enum ShapeType { list, grid }

enum ActionState { create, update, created, updated, init }

enum FormatType { amount, quantity, price, cost, percentage, measurement, int }

enum LogMode { debug, live, info, error, warning }

// enum SaleType { order, invoice, creditMemo }

enum PosmMerchandingType { psom, merchanding, competitorPro }

enum OptionType { qty, uom }

enum OptionShape { row, column }

enum DateType { fromDate, toDate }

enum ListTileType { trailingArrow, trailingSelect }
